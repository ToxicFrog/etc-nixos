// TODO: automatically select file browsing mode and sort by filename
// on load, once I figure out how.
// TODO: add an item to the in-reader menu to window.close() the browser

// alert(JSON.stringify({ ontouchstart: window.ontouchstart,
//   platform: navigator.platform, userAgent: navigator.userAgent,
//   maxTouchPoints: navigator.maxTouchPoints,
//   msMaxTouchPoints: navigator.msMaxTouchPoints }));
